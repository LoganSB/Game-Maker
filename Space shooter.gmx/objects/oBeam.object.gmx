<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>&lt;undefined&gt;</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Initial setup + instant collsion check
beamParent = instance_nearest(x, y, oTurretHardpoint);
temp = false;
damage = 0.0;
baseDamage = 0.3;

rX = x - beamParent.x;
rY = y - beamParent.y;

maxWidth = 9;

width = 1;

life = room_speed / 2;
maxLife = life;

angle = point_direction(x, y, mouse_x, mouse_y);

offscreenCordX = x + 2000 * cos(degtorad(angle));
offscreenCordY = y + 2000 * -sin(degtorad(angle));

incrementInterval = 2;
lastIncrement = 0;

objectHit = 0;

if(objectHit != 0 &amp;&amp; temp == false)
{
    offscreenCordX = objectHit.x;
    offscreenCordY = objectHit.y;
    
    if(instance_nearest(objectHit.x, objectHit.y, oTotalDamageText))
    {
        damageBox = instance_nearest(objectHit.x, objectHit.y, oTotalDamageText)
    }
    
    if(damageBox == 0)
    {
        damageBox = instance_create(objectHit.x, objectHit.y, oTotalDamageText);
    }
    
    damageBox.alarm[0] = damageBox.life;
    damageBox.typeTotal++;
    damageBox.damageAmount += damage;
    damageBox.minDamage = damage * 0.75;
    damageBox.maxDamage = damage * 1.25;
    damageBox.lastDamageInstance = damage;
    
    objectHit.health -= damage;
    temp = true;
}

audio_play_sound(BeamSound, 0, false);
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>x = beamParent.x;
y = beamParent.y;

angle = point_direction(x, y, mouse_x, mouse_y);

if(!beamParent.locked)
{
    offscreenCordX = x + 2000 * cos(degtorad(angle));
    offscreenCordY = y + 2000 * -sin(degtorad(angle));
}
else
{
    angle = beamParent.lockedAngle + beamParent.angle;
    offscreenCordX = x + 2000 * cos(degtorad(angle));
    offscreenCordY = y + 2000 * -sin(degtorad(angle));
}

if(width &lt; maxWidth &amp;&amp; life &gt; 0 &amp;&amp; lastIncrement &gt;= incrementInterval)
{
    width++;
    lastIncrement = 0;
}

else if(width &gt;= maxWidth)
{
    width = maxWidth;
    
    life--;
}

if(life &lt;= 0)
{
    if(width &gt; 0 &amp;&amp; lastIncrement &gt;= incrementInterval)
    {
        width--;
        lastIncrement = 0;
    }
    else if(width &lt;= 0)
    {
        instance_destroy();
    }
}

lastIncrement++;

objectHit = 0;

damageBox = 0;

if(collision_line(x, y, offscreenCordX, offscreenCordY, oEnemy, false, true))
{
    objectHit = collision_line(x, y, offscreenCordX, offscreenCordY, oEnemy, false, true);
}
if(objectHit != 0 &amp;&amp; temp == false)
{
    offscreenCordX = objectHit.x;
    offscreenCordY = objectHit.y;

    if(instance_nearest(objectHit.x, objectHit.y, oTotalDamageText))
    {
        damageBox = instance_nearest(objectHit.x, objectHit.y, oTotalDamageText)
        
        if(damageBox.assosciatedTarget != objectHit)
        {
            damageBox = 0;
        }
    }
    
    if(damageBox == 0)
    {
        damageBox = instance_create(objectHit.x, objectHit.y, oTotalDamageText);
        
        damageBox.assosciatedTarget = objectHit;
    }
    
    damageBox.alarm[0] = damageBox.life;
    damageBox.typeTotal++;
    damageBox.damageAmount += damage;
    damageBox.minDamage = baseDamage * 0.75;
    damageBox.maxDamage = baseDamage * 1.25;
    damageBox.lastDamageInstance = damage;
    
    objectHit.myHealth -= damage;
    temp = true;
}

temp = false;

</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="8" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>///Draw event
draw_set_alpha(width / (maxWidth / 2));

draw_line_width_colour(x, y, offscreenCordX, offscreenCordY, 0.5, c_blue, c_blue);

//draw_set_alpha(width / (maxWidth + 2));
draw_set_alpha(1 / (maxWidth));

for(i = 0; i &lt; width; i++)
{
    draw_line_width_colour(x, y, offscreenCordX, offscreenCordY, i, c_white, c_white);
}

draw_set_alpha(1.0);
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>-1</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
